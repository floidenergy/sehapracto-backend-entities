export enum MetaDataKeys {
  Keys = "metadata:keys",
  Authorization = "authorization",
  Session = "session",
  sessionRole = "x-session-role",
  requiredRole = "x-required-role",
  requestRole = "x-request-role",
}
